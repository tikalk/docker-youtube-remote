# the IP(s) on which your node server is running. I chose port 3000.
upstream yremote {
    server yremote:3000;
}

# the nginx server instance
server {
    listen 81;
    server_name nginx/yremote;
    access_log /var/log/nginx/yremote.log;

    proxy_pass http://yremote/;
  }
}